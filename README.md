# cod-auto-editor

Split version of your original `cod_auto_editor.py`.  
Run:

```bash
python -m cod_auto_editor.cli --input input/match1.mp4 --config config.yml
# or (after pip install -e .)
cod-auto-editor --input input/match1.mp4 --config config.yml
